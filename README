= WebKit RSpec Formatter

WebKit RSpec Formatter is a webkit-aware pretty formatter for RSpec. 

This is a formatter for RSpec that takes advantage of some of the advanced features of the webkit
HTML engine, like the one TextMate uses for its output. 

You can check out the current development source with Subversion from the
following URL:

    svn://deveiate.org/WebKitRSpecFormatter/trunk

The project page is also likely to have more details:

    http://deveiate.org/projects/WebKit-RSpec-Formatter

== License

See the LICENSE file in the same directory for licensing details.
